import 'package:flutter/material.dart';
import 'package:rinavent/core/theme/custom_button_style.dart';
import 'package:rinavent/core/theme/theme_helper.dart';
import 'package:rinavent/core/utils/image_constant.dart';
import 'package:rinavent/core/utils/pref_utils.dart';
import 'package:rinavent/core/utils/size_utils.dart';
import 'package:rinavent/core/presentation/widgets/custom_elevated_button.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:rinavent/core/l10n/app_localizations.dart';

class OnboardingScreen extends StatefulWidget {
  static route() => PageRouteBuilder(pageBuilder: (_, animation, __) {
        return FadeTransition(
          opacity: animation,
          child: const OnboardingScreen(),
        );
      });
  const OnboardingScreen({super.key});

  @override
  State<OnboardingScreen> createState() => _OnboardingScreenState();
}

class _OnboardingScreenState extends State<OnboardingScreen> {
  late PageController _pageController;
  int currentIndex = 0;
  SharedPreferences? prefs;

  @override
  void initState() {
    _pageController = PageController(initialPage: 0);
    pref();
    super.initState();
  }

  void pref() async {
    prefs = await SharedPreferences.getInstance();
  }

  @override
  void dispose() {
    _pageController.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    // final size = MediaQuery.of(context).size;
    var appLocalization = AppLocalizations.of(context);

    const introLength = 3;
    return Scaffold(
      body: Stack(
        alignment: Alignment.bottomCenter,
        children: [
          PageView(
            onPageChanged: (int page) {
              setState(() {
                currentIndex = page;
              });
            },
            controller: _pageController,
            children: [
              // makePage(
              //   image: ImageConstant.imgRectangle34624256,
              //   title: appLocalization!.lbl_make_it_simple,
              //   description: appLocalization.msg_money_transfer_generally,
              // ),
              // makePage(
              //   image: ImageConstant.imgRectangle34624256474x388,
              //   title: appLocalization.lbl_new_banking,
              //   description:
              //       appLocalization.msg_trends_in_this_report,
              // ),
              // makePage(
              //     image: ImageConstant.imgRectangle346242561,
              //     title: appLocalization.lbl_zero_fees,
              //     description: appLocalization.msg_zero_fee_merchant,
              //     button: true,
              //     prefs: prefs),

                        // Image de fond
          SizedBox.expand(
            child: Image.asset(
              'assets/images/concert.jpg', // Remplace par ton image locale
              fit: BoxFit.cover,
            ),
          ),

          // Overlay sombre pour assombrir l'image
          Container(
            color: Colors.black.withOpacity(0.5),
          ),

          // Contenu
          SafeArea(
            child: Padding(
              padding: const EdgeInsets.all(24.0),
              child: Column(
                mainAxisAlignment: MainAxisAlignment.end,
                crossAxisAlignment: CrossAxisAlignment.center,
                children: [
                  Text(
                    'Grab all events now\nonly in your hands',
                    textAlign: TextAlign.center,
                    style: const TextStyle(
                      color: Colors.white,
                      fontSize: 26,
                      fontWeight: FontWeight.bold,
                    ),
                  ),
                  const SizedBox(height: 16),
                  const Text(
                    'Stream is here to help you to find the best events\nbased on your interests.',
                    textAlign: TextAlign.center,
                    style: TextStyle(
                      color: Colors.white70,
                      fontSize: 14,
                    ),
                  ),
                  const SizedBox(height: 40),
                  SizedBox(
                    width: double.infinity,
                    child: ElevatedButton(
                      onPressed: () {
                        // Action bouton
                      },
                      style: ElevatedButton.styleFrom(
                        backgroundColor: Colors.teal, // Couleur bouton
                        shape: RoundedRectangleBorder(
                          borderRadius: BorderRadius.circular(30),
                        ),
                        padding: const EdgeInsets.symmetric(vertical: 16),
                      ),
                      child: const Text(
                        'Next',
                        style: TextStyle(
                          fontSize: 16,
                          fontWeight: FontWeight.bold,
                        ),
                      ),
                    ),
                  ),
                ],
              ),
            ),
          ),
            ],
          ),
          Column(
            mainAxisAlignment: MainAxisAlignment.end,
            children: [
              Row(
                mainAxisAlignment: MainAxisAlignment.center,
                children: List.generate(
                  introLength,
                  (index) {
                    return AnimatedContainer(
                      margin: EdgeInsets.only(left: 4.h, right: 4.h),
                      duration: const Duration(milliseconds: 300),
                      height: 8.h,
                      width: (index == currentIndex) ? 8.h : 8.h,
                      decoration: BoxDecoration(
                          // shape: BoxShape.circle,

                          borderRadius: BorderRadius.circular(16.h),
                          color: (index == currentIndex)
                              ? theme.colorScheme.primary
                              : theme.colorScheme.primary.withValues(alpha: 0.14)),
                    );
                  },
                ),
              ),
              SizedBox(height: 32.v),
              Padding(
                padding: EdgeInsets.symmetric(horizontal: 16.v),
                child: CustomElevatedButton(
                  buttonStyle: CustomButtonStyles.fillPrimary,
                  text: (currentIndex == introLength - 1)
                      ? appLocalization!.lbl_get_started 
                      : appLocalization!.lbl_next ,
                  onPressed: currentIndex == introLength - 1
                      ? () {
                          // PrefUtils.setIntro(false);
                  // Navigator.push(context, LoadingScreen.route());
                        }
                      : () {
                          _pageController.nextPage(
                              duration: const Duration(milliseconds: 100),
                              curve: Curves.bounceIn);
                        },
                ),
              ),
              SizedBox(height: 16.v),
              InkWell(
                onTap: () {
                  PrefUtils.setIntro(false);
                  PrefUtils.setSelectedFlag("SN");
                      // Navigator.pushAndRemoveUntil(
                      //     context,
                      //     LoadingCurrencyScreen.route(),
                      //     (Route<dynamic> route) => false, 
                      //   );
                },
                child: Text(
                  (currentIndex == introLength - 1) ? " " : appLocalization.lbl_skip,
                  style: theme.textTheme.bodyLarge,
                ),
              ),
              SizedBox(height: 40.v),
            ],
          )
        ],
      ),
    );
  }

  Widget makePage(
      {image, title, description, button = false, SharedPreferences? prefs}) {
    return 
    Container(
      padding: const EdgeInsets.only(left: 33, right: 33, bottom: 50),
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Padding(
            padding: const EdgeInsets.symmetric(horizontal: 20),
            child: SizedBox(
              width: double.infinity,
              height: MediaQuery.of(context).size.height * 0.4,
             
              child: ClipRRect(
                borderRadius: BorderRadius.circular(8),
                child: Image.asset(
                   image,
                   fit: BoxFit.cover,
                ),
              ),
            ),
          ),
          const SizedBox(
            height: 30,
          ),
          Text(
            title,
            style: theme.textTheme.headlineMedium!.copyWith(
                  height: 1.36,
                  fontSize: 34.fSize,
                ),
            textAlign: TextAlign.center,
          ),
          const SizedBox(
            height: 30,
          ),
          Text(
            description,
            style: theme.textTheme.bodyLarge!.copyWith(
                height: 1.50,
                fontSize: 17.fSize,
              ),
            textAlign: TextAlign.center,
          ),
          const SizedBox(
            height: 30,
          ),
         
        ],
      ),
    );
  }
}
